@model List<User>

<h2>Lista de Utilizatori</h2>

<table class="table">
    <a asp-controller="Account" asp-action="Signup">Add user</a>
    <thead>
        <tr>
            <th>ID</th>
            <th>Email</th>
            <th>Is Admin</th>
        </tr>
    </thead>
    <tbody>
       @foreach (var user in Model)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.Email</td>
                <td>
                    <form method="post" asp-action="DeleteUser" asp-route-userId="@user.Id">
                        <button type="submit">Sterge Utilizator</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

